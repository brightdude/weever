#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../total.js/bin/tpm" "$@"
  ret=$?
else 
  node  "$basedir/../total.js/bin/tpm" "$@"
  ret=$?
fi
exit $ret
